import express from 'express';import path from 'node:path';import {fileURLToPath} from 'node:url';const app=express();const port=Number(process.env.PORT||8788);app.use(express.json());app.get('/api/health',(_req,res)=>res.json({status:'ok',service:'threadline',auth:process.env.FIREBASE_PROJECT_ID?'firebase':'demo',timestamp:new Date().toISOString()}));const here=path.dirname(fileURLToPath(import.meta.url));const client=path.resolve(here,'../dist');app.use(express.static(client));app.get(/.*/,(_req,res)=>res.sendFile(path.join(client,'index.html')));app.listen(port,'0.0.0.0',()=>console.log(`Threadline listening on ${port}`));
