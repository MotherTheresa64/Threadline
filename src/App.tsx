import {useEffect,useMemo,useState} from 'react';import {Search,Plus,Hash,Inbox,Bookmark,Compass,Settings,Bell,ChevronDown,MessageSquare,CheckCircle2,ThumbsUp,Reply,MoreHorizontal,PanelLeft,PenLine,Users,Clock3,Sparkles,X,Send,Archive,Link2} from 'lucide-react';
type ReplyT={id:string;author:string;initials:string;time:string;body:string};type Thread={id:string;channel:string;title:string;body:string;author:string;initials:string;time:string;tags:string[];replies:ReplyT[];likes:number;views:number;saved:boolean;solved?:boolean};
const seed:Thread[]=[{id:'1',channel:'engineering',title:'What should our API retry policy look like?',body:'We currently retry all 5xx responses with the same backoff. I think we should separate transient gateway failures from application errors and add jitter before this goes into the mobile release.',author:'Maya Chen',initials:'MC',time:'18m',tags:['api','reliability'],likes:14,views:86,saved:true,solved:true,replies:[{id:'r1',author:'Alex Rivera',initials:'AR',time:'12m',body:'I would cap client retries at 2 and move any longer retry strategy to the job layer. That keeps request latency predictable.'},{id:'r2',author:'Noah Ragan',initials:'NR',time:'6m',body:'Agreed. We can also honor Retry-After when upstream gives us one and document which error classes are safe to replay.'}]},{id:'2',channel:'product',title:'Decision: simplify the free workspace limits',body:'Posting the outcome from today’s pricing review so we have one durable source of truth. Free workspaces will keep unlimited viewers and cap active editors at three.',author:'Jamie Lee',initials:'JL',time:'42m',tags:['decision','pricing'],likes:21,views:132,saved:false,solved:true,replies:[{id:'r3',author:'Noah Ragan',initials:'NR',time:'31m',body:'This makes the upgrade boundary a lot easier to explain in-product. I’ll update the empty state copy.'}]},{id:'3',channel:'design',title:'Small accessibility audit findings',body:'I ran a keyboard-only pass on the settings area. Most flows are solid, but modal focus restoration and two icon-only actions need attention before release.',author:'Avery Morgan',initials:'AM',time:'1h',tags:['a11y','design-system'],likes:9,views:54,saved:false,replies:[]},{id:'4',channel:'engineering',title:'Patterns for optimistic updates with rollback',body:'Collecting examples of where we already use optimistic UI successfully and where we should avoid it. Comments and reactions feel safe; billing settings probably do not.',author:'Noah Ragan',initials:'NR',time:'2h',tags:['frontend','architecture'],likes:18,views:110,saved:true,replies:[{id:'r4',author:'Maya Chen',initials:'MC',time:'1h',body:'A small mutation state machine would make rollback behavior consistent without tying us to a specific data library.'}]},{id:'5',channel:'research',title:'Customer interview notes: onboarding week 34',body:'Three recurring themes this week: people understand projects quickly, invite teammates later than expected, and want examples before creating their first workflow.',author:'Sam Kim',initials:'SK',time:'3h',tags:['research','onboarding'],likes:11,views:70,saved:false,replies:[]}];
const channels=[['engineering',12],['product',6],['design',4],['research',3],['random',0]] as const;const STORE='threadline-v1';
export default function App(){const [threads,setThreads]=useState<Thread[]>(()=>{try{return JSON.parse(localStorage.getItem(STORE)||'null')||seed}catch{return seed}});const [selected,setSelected]=useState('1');const [channel,setChannel]=useState('engineering');const [query,setQuery]=useState('');const [composer,setComposer]=useState(false);const [mobileNav,setMobileNav]=useState(false);const [toast,setToast]=useState('');useEffect(()=>localStorage.setItem(STORE,JSON.stringify(threads)),[threads]);useEffect(()=>{if(!toast)return;const t=setTimeout(()=>setToast(''),2200);return()=>clearTimeout(t)},[toast]);const visible=useMemo(()=>threads.filter(t=>(channel==='all'||t.channel===channel)&&(!query||`${t.title} ${t.body} ${t.tags.join(' ')} ${t.author}`.toLowerCase().includes(query.toLowerCase()))),[threads,channel,query]);const active=threads.find(t=>t.id===selected)||visible[0];const toggleSave=(id:string)=>setThreads(v=>v.map(t=>t.id===id?{...t,saved:!t.saved}:t));const like=(id:string)=>setThreads(v=>v.map(t=>t.id===id?{...t,likes:t.likes+1}:t));const reply=(id:string,body:string)=>setThreads(v=>v.map(t=>t.id===id?{...t,replies:[...t.replies,{id:crypto.randomUUID(),author:'Noah Ragan',initials:'NR',time:'now',body}]}:t));return <div className="shell"><aside className={mobileNav?'rail open':'rail'}><div className="logo"><span>t</span>threadline</div><button className="workspace"><span className="avatar nr">NR</span><span><b>Northstar Labs</b><small>8 members</small></span><ChevronDown size={15}/></button><nav><button><Inbox/><span>Inbox</span><b>7</b></button><button><Bookmark/><span>Saved</span></button><button><Compass/><span>Explore</span></button></nav><div className="nav-head">Channels <Plus size={14}/></div><div className="channels"><button className={channel==='all'?'active':''} onClick={()=>setChannel('all')}><Hash/>all threads</button>{channels.map(([c,n])=><button className={channel===c?'active':''} key={c} onClick={()=>{setChannel(c);setMobileNav(false)}}><Hash/>{c}{n>0&&<b>{n}</b>}</button>)}</div><div className="people"><div className="nav-head">Online now <span>4</span></div>{[['MC','Maya'],['AM','Avery'],['JL','Jamie']].map(([a,n])=><div key={a}><span className="avatar">{a}<i/></span>{n}</div>)}</div><button className="settings"><Settings/>Workspace settings</button></aside><main className="feed"><header><button className="mobile" onClick={()=>setMobileNav(v=>!v)}><PanelLeft/></button><div className="search"><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search conversations, decisions, people…"/><kbd>⌘ K</kbd></div><button className="bell"><Bell/><i/></button><button className="new" onClick={()=>setComposer(true)}><PenLine/>New thread</button></header><section className="feed-head"><div><span className="eyebrow">Workspace discussions</span><h1>#{channel==='all'?'all':channel}</h1><p>{channel==='engineering'?'Architecture, implementation, incidents, and useful technical context.':'Shared context your team can find again later.'}</p></div><div className="faces"><span>MC</span><span>AR</span><span>NR</span><b>+5</b></div></section><div className="filters"><button className="active">Latest</button><button>Popular</button><button>Unanswered</button><span>{visible.length} threads</span></div><div className="thread-list">{visible.map(t=><button key={t.id} className={selected===t.id?'thread selected':'thread'} onClick={()=>setSelected(t.id)}><div className={`avatar big a${t.initials.charCodeAt(0)%4}`}>{t.initials}</div><div><div className="thread-meta"><b>{t.author}</b><span>{t.time} ago · #{t.channel}</span>{t.solved&&<em><CheckCircle2/>Resolved</em>}</div><h2>{t.title}</h2><p>{t.body}</p><div className="tags">{t.tags.map(x=><span key={x}>{x}</span>)}</div><div className="stats"><span><ThumbsUp/>{t.likes}</span><span><MessageSquare/>{t.replies.length}</span><span><Clock3/>{t.views} views</span></div></div><button className={t.saved?'save saved':'save'} onClick={e=>{e.stopPropagation();toggleSave(t.id)}}><Bookmark/></button></button>)}</div></main><aside className="detail">{active?<ThreadDetail thread={active} onLike={()=>like(active.id)} onSave={()=>toggleSave(active.id)} onReply={b=>{reply(active.id,b);setToast('Reply posted')}}/>:<div className="empty">No thread selected.</div>}</aside>{composer&&<Composer channel={channel==='all'?'engineering':channel} onClose={()=>setComposer(false)} onCreate={t=>{setThreads(v=>[t,...v]);setSelected(t.id);setChannel(t.channel);setComposer(false);setToast('Thread published')}}/>}{toast&&<div className="toast"><CheckCircle2/>{toast}</div>}</div>}
function ThreadDetail({thread,onLike,onSave,onReply}:{thread:Thread;onLike:()=>void;onSave:()=>void;onReply:(b:string)=>void}){const [body,setBody]=useState('');return <div className="detail-inner"><div className="detail-top"><span>Thread</span><div><button onClick={onSave} className={thread.saved?'active':''}><Bookmark/></button><button><MoreHorizontal/></button></div></div><div className="author"><span className="avatar big">{thread.initials}</span><div><b>{thread.author}</b><small>#{thread.channel} · {thread.time} ago</small></div></div><h2>{thread.title}</h2><p className="body">{thread.body}</p><div className="detail-actions"><button onClick={onLike}><ThumbsUp/>{thread.likes}</button><button><Reply/>Reply</button><button><Link2/>Copy link</button></div>{thread.solved&&<div className="resolved"><CheckCircle2/><div><b>Marked as resolved</b><span>This thread contains a confirmed answer or decision.</span></div></div>}<div className="reply-head"><b>{thread.replies.length} {thread.replies.length===1?'reply':'replies'}</b><span>Newest first</span></div><div className="replies">{thread.replies.slice().reverse().map(r=><article key={r.id}><span className="avatar">{r.initials}</span><div><b>{r.author}<small>{r.time} ago</small></b><p>{r.body}</p><button><ThumbsUp/>Helpful</button></div></article>)}</div><form className="reply-box" onSubmit={e=>{e.preventDefault();if(body.trim()){onReply(body.trim());setBody('')}}}><span className="avatar nr">NR</span><div><textarea value={body} onChange={e=>setBody(e.target.value)} placeholder="Add useful context…"/><div><span>Markdown supported</span><button><Send/>Reply</button></div></div></form></div>}
function Composer({channel,onClose,onCreate}:{channel:string;onClose:()=>void;onCreate:(t:Thread)=>void}){const [title,setTitle]=useState('');const [body,setBody]=useState('');const [ch,setCh]=useState(channel);const [tags,setTags]=useState('');return <div className="overlay" onMouseDown={onClose}><form className="composer" onMouseDown={e=>e.stopPropagation()} onSubmit={e=>{e.preventDefault();if(title.trim()&&body.trim())onCreate({id:crypto.randomUUID(),channel:ch,title:title.trim(),body:body.trim(),author:'Noah Ragan',initials:'NR',time:'now',tags:tags.split(',').map(x=>x.trim()).filter(Boolean),likes:0,views:1,saved:false,replies:[]})}}><button type="button" className="close" onClick={onClose}><X/></button><span className="compose-icon"><Sparkles/></span><h2>Start a useful thread</h2><p>Ask a question, document a decision, or leave context future-you will be glad to find.</p><label>Title<input autoFocus value={title} onChange={e=>setTitle(e.target.value)} placeholder="What should people know?"/></label><label>Channel<select value={ch} onChange={e=>setCh(e.target.value)}>{channels.map(([c])=><option key={c}>{c}</option>)}</select></label><label>Details<textarea value={body} onChange={e=>setBody(e.target.value)} placeholder="Add enough context for someone who wasn't in the room…"/></label><label>Tags<input value={tags} onChange={e=>setTags(e.target.value)} placeholder="api, reliability, decision"/></label><button className="publish"><Send/>Publish thread</button></form></div>}
